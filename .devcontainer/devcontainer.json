{
    "name": "Node.js",
    "build": {
        "dockerfile": "./Dockerfile"
    },
    "forwardPorts": [],
    "runArgs": ["--network=host"],
    "remoteUser": "root",
    "customizations": {
        "vscode": {
            "extensions": [
                "trufflesuite-csi.truffle-vscode",
                "EditorConfig.EditorConfig"
            ]
        }
    }
}
